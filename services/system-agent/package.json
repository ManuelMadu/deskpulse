{
  "name": "@deskpulse/system-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Node.js system agent: metrics sampling, log tailing, health probes, SSE event stream, diagnostics export. No Electron dependency — ever.",
  "type": "module",
  "scripts": {
    "build": "tsc -b && node esbuild.config.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@deskpulse/contracts": "*",
    "archiver": "^6.0.2",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/archiver": "^8.0.0",
    "esbuild": "^0.28.1"
  }
}
